# Frontend application URL for CORS
FRONTEND_URL=http://localhost:5173

# Database connection string (if using real database)
# DATABASE_CONNECTION_STRING=Server=localhost;Database=PackageDb;Trusted_Connection=true;TrustServerCertificate=true;

# Environment
ASPNETCORE_ENVIRONMENT=Development